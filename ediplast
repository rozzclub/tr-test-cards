$ apt update -y
$ apt upgrade -y 
$ apt install python3 -y
$ apt install git -y
$ git clone https://github.com/escdroid/cardesc
$ cd cardesc
$ chmod +x *
$ pip3 install -r requirements.txt
$ python3 bank.py
